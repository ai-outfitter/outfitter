{
  "name": "@ai-outfitter/agentic-evals",
  "private": true,
  "version": "0.0.0",
  "description": "Profile-driven end-to-end evals: run a workflow with a profile, assert the expected artifact.",
  "type": "module",
  "bin": {
    "agentic-evals": "src/cli.ts"
  },
  "scripts": {
    "evals": "node src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run",
    "coverage": "vitest --run --coverage"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "@vitest/coverage-v8": "^4.1.7",
    "typescript": "^5.9.3",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=22.19.0"
  }
}
