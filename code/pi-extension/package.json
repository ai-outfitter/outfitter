{
  "name": "@ai-outfitter/pi-extension",
  "version": "0.6.1",
  "private": true,
  "description": "Source of the Pi-native Outfitter bootstrap extension, stamped and loaded by the Outfitter CLI at launch.",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('pi-extension source ships unbuilt; the CLI stamps runtime values at launch')\"",
    "lint": "node --check src/outfitter-extension.js",
    "test": "node -e \"console.log('pi-extension behavior is covered by code/cli/tests/unit/pi-login-launch.test.ts')\""
  }
}
